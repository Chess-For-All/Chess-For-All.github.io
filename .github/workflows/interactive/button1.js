var x = 0

function click() {
  for (infinity) {
    if (onclick) {
      x = x + 1;
    } else { 
      x = x + 0;
    }
  }
}

const element = document.getElementById('like')
element.style.text = 'like'
